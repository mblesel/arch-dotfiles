#!/usr/bin/env bash

# wal -i "$1" -nq
# ln -sf ~/.cache/wal/dunstrc ~/.config/dunst/dunstrc
# killall dunst
# killall -SIGUSR2 waybar

killall swaybg
echo "$1" > ~/.wallpaper
swaybg -m fill -i "$1"

